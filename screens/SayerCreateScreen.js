import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const SayerCreateScreen = props => {
    return (
        <View>
            <Text>SayerCreateScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({

});

export default SayerCreateScreen;